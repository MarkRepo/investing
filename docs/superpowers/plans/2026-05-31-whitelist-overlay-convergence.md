# 域族 Whitelist Overlay 收敛回路 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让主 agent 每次 H2 救回判出的 `llm-judged-official` 权威源，按"域族"自动沉淀进一张可继承的 overlay 白名单，使同族 topic 跑得越多、手判越少（治 PRISM_VALIDATION F4 的复发税 + "无收敛"缺陷）。

**Architecture:** 分层白名单 = 全局核心表 `WHITELIST_DOMAINS`（代码内，跨族通用，几乎不增长）+ 族 overlay（`prism/state/whitelist/overlays/{family}.json`，磁盘，随判断累积）。`classify_domain` 增 `family` 参数，命中顺序 = 全局表 → 族 overlay → IR 启发式。收敛回路：主 agent 调 `register_web_search_batch(..., domain_tier='llm-judged-official')` 时，脚本侧 `_promote` bump 一个跨 topic 的 promotion log，同一 host 在同族被判 official ≥ N 次（N=2）即自动写进 overlay。**主 agent 全程不 Read overlay/log 文件——脚本当 oracle，零额外上下文。** 族 key 在 prescan 阶段（sidecar 尚未生成）取 `parent_topic || slug`，create_topic 时即可用。

**Tech Stack:** Python 3（`pathlib` / `json` / 既有 `prism.scripts.web_prescan` + `prism.scripts.topic`）；pytest（既有 `prism/scripts/test_*.py` 约定，直接 `from prism.scripts.web_prescan import ...`）。

**关键既有事实（已核实）：**
- `web_prescan.py:25` `PRISM_ROOT = Path(__file__).resolve().parent.parent` → `prism/`，故状态目录 = `prism/state/whitelist/`。
- `classify_domain(url)`（`web_prescan.py:145`）当前 = 全局表 exact → endswith → IR 启发式。内部调用点仅 2 处：`extract_url_features`（`:265`）、`register_web_search_result`（`:478`）。gitnexus impact = LOW（无跨文件 caller）。
- `register_web_search_result(slug, variant, ...)`（`:443`）已持有 slug+variant，可内部派生 family，主 agent 零额外参数。
- `register_web_search_batch`（`:594`）逐条调 `register_web_search_result`——救回路径（`domain_tier='llm-judged-official'`）就走这里，是唯一晋升触发点。
- `topic.read_topic(slug, variant)` 返回含 `parent_topic` 键的 dict（`topic.py:293` 写入，`:314` setdefault None）。

---

## File Structure

| File | 责任 | 动作 |
|---|---|---|
| `prism/scripts/web_prescan.py` | overlay 存储/加载、family 派生、`classify_domain` family-aware、`_promote` 回路、register/extract 接线 | Modify |
| `prism/scripts/test_whitelist_overlay.py` | 本特性全部单测（overlay 加载/缓存、family-aware 分类、family 派生、promotion 阈值与去重、register 端到端晋升） | Create |
| `prism/scripts/seed_overlay_cn_pharma.py` | 一次性冷启动种子：把 F4 已判的 11 个缺失权威 host 灌进 `cn-innovative-drug.json` overlay + 标 log promoted | Create |
| `prism/state/whitelist/overlays/cn-innovative-drug.json` | 种子产物（learned 资产，**入库**） | Generated by seed |
| `prism/state/whitelist/_promotion_log.json` | 跨 topic 计数账本（learned 资产，**入库**） | Generated by seed |
| `prism/scripts/test_whitelist_domains.py` | 既有全局表 sanity 测试 | 不改（family=None 向后兼容须保持全绿） |

**状态文件是否入库：** overlay 和 log 是系统"学到的白名单"，是收敛资产，**提交进仓库**（不要 gitignore）。这样团队/后续会话共享已收敛结果。

---

## Task 1: overlay 存储与加载（`_STATE_DIR` / `_load_overlay` / `_matches` 重构）

**Files:**
- Modify: `prism/scripts/web_prescan.py`（在 `WHITELIST_DOMAINS` 块之后、`classify_domain` 之前插入）
- Test: `prism/scripts/test_whitelist_overlay.py`

把 `classify_domain` 里 exact+endswith 的匹配逻辑抽成共享 `_matches`，新增可 monkeypatch 的 `_STATE_DIR`、按 family 缓存的 `_load_overlay`。

- [ ] **Step 1: Write the failing test**

创建 `prism/scripts/test_whitelist_overlay.py`：

```python
"""域族 whitelist overlay 收敛回路 单测。"""
import json
import pytest

from prism.scripts import web_prescan as wp


@pytest.fixture(autouse=True)
def isolated_state(tmp_path, monkeypatch):
    """每个测试用独立 state 目录 + 清空 overlay 内容缓存，绝不碰真实 prism/state。"""
    state = tmp_path / "whitelist"
    (state / "overlays").mkdir(parents=True)
    monkeypatch.setattr(wp, "_STATE_DIR", state)
    wp._OVERLAY_CACHE.clear()
    yield state
    wp._OVERLAY_CACHE.clear()


def _write_overlay(state, family, hosts):
    p = state / "overlays" / f"{family}.json"
    p.write_text(json.dumps({"family": family, "hosts": hosts,
                             "promoted_count": len(hosts)}), encoding="utf-8")


def test_matches_exact_and_suffix():
    hosts = {"hkexnews.hk", "hengrui.com"}
    assert wp._matches("hkexnews.hk", hosts) is True
    assert wp._matches("www1.hengrui.com", hosts) is True   # endswith ".hengrui.com"
    assert wp._matches("nothengrui.com", hosts) is False
    assert wp._matches("random.example", hosts) is False


def test_load_overlay_reads_file(isolated_state):
    _write_overlay(isolated_state, "cn-pharma", ["hkexnews.hk"])
    assert wp._load_overlay("cn-pharma") == {"hkexnews.hk"}


def test_load_overlay_missing_returns_empty(isolated_state):
    assert wp._load_overlay("no-such-family") == set()


def test_load_overlay_caches(isolated_state):
    _write_overlay(isolated_state, "cn-pharma", ["a.com"])
    assert wp._load_overlay("cn-pharma") == {"a.com"}
    # 改盘后不失效缓存 → 仍读到旧值（证明确实走缓存）
    _write_overlay(isolated_state, "cn-pharma", ["a.com", "b.com"])
    assert wp._load_overlay("cn-pharma") == {"a.com"}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd /Users/yangqi/investing && python3 -m pytest prism/scripts/test_whitelist_overlay.py -v`
Expected: FAIL with `AttributeError: module ... has no attribute '_STATE_DIR'` / `_OVERLAY_CACHE` / `_matches` / `_load_overlay`.

- [ ] **Step 3: Write minimal implementation**

在 `web_prescan.py` 顶部 import 区补 `import json`（确认 `from pathlib import Path` 已在 `:14`）。在 `_IR_SUBDOMAIN_TOKENS = (...)`（`:131`）之后、`_domain_of`（`:134`）之前插入：

```python
# ---------------------------------------------------------------------------
# 域族 overlay 收敛回路（PRISM_VALIDATION F4 修）—— 分层白名单的"族"层
# 全局核心表 WHITELIST_DOMAINS 跨族通用、几乎不增长；族 overlay 随主 agent 的
# llm-judged-official 判断累积。脚本当 oracle，overlay/log 永不进主 agent 上下文。
# ---------------------------------------------------------------------------
import json  # noqa: E402  (若文件顶部已 import json，删除本行)

_STATE_DIR = PRISM_ROOT / "state" / "whitelist"
PROMOTION_THRESHOLD = 2          # 同族同 host 被判 official ≥ N 次 → 晋升进 overlay
_OVERLAY_CACHE: dict[str, set[str]] = {}   # family -> hosts，进程内内容缓存


def _matches(domain: str, hosts: set[str]) -> bool:
    """exact 命中或作为某 host 的子域（endswith '.<host>'）。"""
    if not domain:
        return False
    if domain in hosts:
        return True
    return any(domain.endswith("." + h) for h in hosts)


def _overlay_path(family: str) -> Path:
    return _STATE_DIR / "overlays" / f"{family}.json"


def _load_overlay(family: str) -> set[str]:
    """读族 overlay 的 hosts 集（带进程内缓存）。缺文件 → 空集，不抛。"""
    if family in _OVERLAY_CACHE:
        return _OVERLAY_CACHE[family]
    hosts: set[str] = set()
    p = _overlay_path(family)
    if p.is_file():
        try:
            hosts = {str(h) for h in (json.loads(p.read_text(encoding="utf-8")).get("hosts") or [])}
        except Exception:
            hosts = set()
    _OVERLAY_CACHE[family] = hosts
    return hosts
```

> 注意：`import json` 若文件顶部（`:11-15` 区）已存在则删掉这行内联 import，避免重复。实现前先 grep `^import json` 确认。

- [ ] **Step 4: Run test to verify it passes**

Run: `cd /Users/yangqi/investing && python3 -m pytest prism/scripts/test_whitelist_overlay.py -v`
Expected: 4 passed（`test_matches_exact_and_suffix` / `test_load_overlay_reads_file` / `test_load_overlay_missing_returns_empty` / `test_load_overlay_caches`）。

- [ ] **Step 5: Commit**

```bash
cd /Users/yangqi/investing
git add prism/scripts/web_prescan.py prism/scripts/test_whitelist_overlay.py
git commit -m "feat(prism): overlay 存储+加载基建（F4 收敛回路 1/4）"
```

---

## Task 2: `classify_domain` family-aware（向后兼容）

**Files:**
- Modify: `prism/scripts/web_prescan.py:145-165`（`classify_domain`）
- Test: `prism/scripts/test_whitelist_overlay.py`

- [ ] **Step 1: Write the failing test**

在 `test_whitelist_overlay.py` 追加：

```python
def test_classify_uses_overlay_only_with_family(isolated_state):
    _write_overlay(isolated_state, "cn-pharma", ["hkexnews.hk"])
    url = "https://www1.hkexnews.hk/listedco/x.htm"
    # 不传 family → 仍是 other（全局表里没有）
    assert wp.classify_domain(url) == "other"
    # 传 family → overlay 命中 → whitelist
    assert wp.classify_domain(url, family="cn-pharma") == "whitelist"


def test_classify_global_still_wins_without_family():
    # 全局表成员不依赖 family（向后兼容）
    assert wp.classify_domain("https://www.csrc.gov.cn/x") == "whitelist"
    assert wp.classify_domain("https://ir.tencent.com/x") == "whitelist"  # IR 启发式
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd /Users/yangqi/investing && python3 -m pytest prism/scripts/test_whitelist_overlay.py::test_classify_uses_overlay_only_with_family -v`
Expected: FAIL — `classify_domain() got an unexpected keyword argument 'family'`。

- [ ] **Step 3: Write minimal implementation**

把 `classify_domain`（`:145-165`）整体替换为（复用 `_matches`，新增 overlay 分支）：

```python
def classify_domain(url: str, family: str | None = None) -> str:
    """Return 'whitelist' / 'llm-judged-official' / 'other'.

    命中顺序：① 全局核心表 WHITELIST_DOMAINS → ② 族 overlay（仅当传 family）
    → ③ IR 子域启发式。family=None 时行为与历史完全一致（向后兼容）。

    'llm-judged-official' 只在 caller 显式传 domain_tier 时出现，本函数不从 URL 产出它。
    """
    domain = _domain_of(url)
    if not domain:
        return "other"
    if _matches(domain, WHITELIST_DOMAINS):              # ① 全局核心表
        return "whitelist"
    if family and _matches(domain, _load_overlay(family)):  # ② 族 overlay（F4 收敛）
        return "whitelist"
    if any(tok in domain for tok in _IR_SUBDOMAIN_TOKENS):  # ③ IR 子域启发式
        return "whitelist"
    return "other"
```

- [ ] **Step 4: Run test to verify it passes（含既有全局表测试不回归）**

Run: `cd /Users/yangqi/investing && python3 -m pytest prism/scripts/test_whitelist_overlay.py prism/scripts/test_whitelist_domains.py prism/scripts/test_url_features.py -v`
Expected: 全 passed（新增 2 条 + 既有 `test_whitelist_domains` / `test_url_features` 全绿，证明 family=None 无回归）。

- [ ] **Step 5: Commit**

```bash
cd /Users/yangqi/investing
git add prism/scripts/web_prescan.py prism/scripts/test_whitelist_overlay.py
git commit -m "feat(prism): classify_domain 加 family overlay 分支（F4 收敛回路 2/4）"
```

---

## Task 3: family 派生 `_family_of`（prescan 阶段可用）

**Files:**
- Modify: `prism/scripts/web_prescan.py`（紧接 `_load_overlay` 之后新增）
- Test: `prism/scripts/test_whitelist_overlay.py`

族 key = `parent_topic || slug`：industry 根 topic 用自身 slug；其 arena/company 子 topic 的 `parent_topic` 指向同一 industry slug → 自动收敛到同一 overlay 文件。**不依赖 sidecar cluster_tags**（prescan 阶段 sidecar 尚未生成）。

- [ ] **Step 1: Write the failing test**

追加（monkeypatch `read_topic` 隔离磁盘）：

```python
def test_family_of_root_uses_slug(monkeypatch):
    monkeypatch.setattr(wp.topic_io, "read_topic",
                        lambda s, v: {"type": "industry", "parent_topic": None})
    assert wp._family_of("cn-innovative-drug", "opus4.8") == "cn-innovative-drug"


def test_family_of_child_uses_parent(monkeypatch):
    monkeypatch.setattr(wp.topic_io, "read_topic",
                        lambda s, v: {"type": "arena", "parent_topic": "cn-innovative-drug"})
    assert wp._family_of("adc-arena", "v1") == "cn-innovative-drug"


def test_family_of_read_error_returns_none(monkeypatch):
    def boom(s, v):
        raise FileNotFoundError
    monkeypatch.setattr(wp.topic_io, "read_topic", boom)
    assert wp._family_of("ghost", "v1") is None
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd /Users/yangqi/investing && python3 -m pytest prism/scripts/test_whitelist_overlay.py::test_family_of_root_uses_slug -v`
Expected: FAIL — `module 'prism.scripts.web_prescan' has no attribute '_family_of'`。

- [ ] **Step 3: Write minimal implementation**

在 `_load_overlay` 之后插入（注意 `topic_io` 已在 `web_prescan.py:19` `from prism.scripts import topic as topic_io` 导入）：

```python
def _family_of(slug: str, variant: str) -> str | None:
    """域族 key = parent_topic || slug。

    prescan 阶段 sidecar 未生成、cluster_tags 不可得，故用 create_topic 即写入的
    parent_topic（无父的 industry 根 topic 用自身 slug）。读不到 topic → None
    （= 退化为全局表 only，行为同历史）。
    """
    try:
        topic = topic_io.read_topic(slug, variant)
    except Exception:
        return None
    return topic.get("parent_topic") or slug
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd /Users/yangqi/investing && python3 -m pytest prism/scripts/test_whitelist_overlay.py -v -k family_of`
Expected: 3 passed。

- [ ] **Step 5: Commit**

```bash
cd /Users/yangqi/investing
git add prism/scripts/web_prescan.py prism/scripts/test_whitelist_overlay.py
git commit -m "feat(prism): _family_of 派生（parent_topic||slug，F4 收敛回路 3/4）"
```

---

## Task 4: promotion log + `_promote` 晋升回路

**Files:**
- Modify: `prism/scripts/web_prescan.py`（紧接 `_family_of` 之后新增）
- Test: `prism/scripts/test_whitelist_overlay.py`

log schema：`{family: {host: {count, topics:[...], promoted: bool}}}`。同一 topic 内重复判同一 host 只记 1（防单 topic 刷 count 假晋升）；`count ≥ PROMOTION_THRESHOLD` 且未晋升 → append 进 overlay + 置 `promoted=True` + 失效内容缓存。

- [ ] **Step 1: Write the failing test**

追加：

```python
def test_promote_below_threshold_no_overlay(isolated_state):
    promoted = wp._promote("cn-pharma", "hengrui.com", "cn-innovative-drug/opus4.8")
    assert promoted is False
    assert not wp._overlay_path("cn-pharma").exists()   # 阈值=2，1 次不建 overlay
    log = json.loads((isolated_state / "_promotion_log.json").read_text())
    assert log["cn-pharma"]["hengrui.com"]["count"] == 1


def test_promote_same_topic_does_not_double_count(isolated_state):
    wp._promote("cn-pharma", "hengrui.com", "T1/v1")
    promoted = wp._promote("cn-pharma", "hengrui.com", "T1/v1")  # 同 topic 重复
    assert promoted is False
    log = json.loads((isolated_state / "_promotion_log.json").read_text())
    assert log["cn-pharma"]["hengrui.com"]["count"] == 1         # 仍是 1


def test_promote_reaches_threshold_writes_overlay(isolated_state):
    wp._promote("cn-pharma", "hengrui.com", "T1/v1")
    promoted = wp._promote("cn-pharma", "hengrui.com", "T2/v1")  # 第 2 个不同 topic
    assert promoted is True
    assert wp._load_overlay("cn-pharma") == {"hengrui.com"}      # 已进 overlay
    # 晋升后 classify 立即生效（缓存已失效）
    assert wp.classify_domain("https://hengrui.com/ir", family="cn-pharma") == "whitelist"
    log = json.loads((isolated_state / "_promotion_log.json").read_text())
    assert log["cn-pharma"]["hengrui.com"]["promoted"] is True


def test_promote_already_promoted_is_noop_after_threshold(isolated_state):
    wp._promote("cn-pharma", "hengrui.com", "T1/v1")
    wp._promote("cn-pharma", "hengrui.com", "T2/v1")             # → promoted
    promoted = wp._promote("cn-pharma", "hengrui.com", "T3/v1")  # 已晋升
    assert promoted is False                                     # 不重复写


def test_append_overlay_sets_and_preserves_display_name(isolated_state):
    # 显式传 display_name → 写入；后续 _promote 自动建（不传）→ 不覆盖已有中文名
    wp._append_to_overlay("cn-pharma", "a.com", display_name="中国创新药")
    wp._append_to_overlay("cn-pharma", "b.com")                  # 无 display_name
    import json as _json
    doc = _json.loads(wp._overlay_path("cn-pharma").read_text(encoding="utf-8"))
    assert doc["display_name"] == "中国创新药"                    # 保留
    assert set(doc["hosts"]) == {"a.com", "b.com"}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd /Users/yangqi/investing && python3 -m pytest prism/scripts/test_whitelist_overlay.py -v -k promote`
Expected: FAIL — `module ... has no attribute '_promote'`。

- [ ] **Step 3: Write minimal implementation**

在 `_family_of` 之后插入：

```python
def _promotion_log_path() -> Path:
    return _STATE_DIR / "_promotion_log.json"


def _read_promotion_log() -> dict:
    p = _promotion_log_path()
    if not p.is_file():
        return {}
    try:
        return json.loads(p.read_text(encoding="utf-8")) or {}
    except Exception:
        return {}


def _write_json_atomic(path: Path, data: dict) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    tmp = path.with_suffix(path.suffix + ".tmp")
    tmp.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")
    tmp.replace(path)


def _append_to_overlay(family: str, host: str, display_name: str | None = None) -> None:
    p = _overlay_path(family)
    doc = {"family": family, "display_name": None, "hosts": [], "promoted_count": 0}
    if p.is_file():
        try:
            doc = json.loads(p.read_text(encoding="utf-8"))
        except Exception:
            pass
    hosts = sorted(set(doc.get("hosts") or []) | {host})
    doc["hosts"] = hosts
    doc["promoted_count"] = len(hosts)
    doc["family"] = family
    # display_name 是给人看的中文标签（机器键仍是英文 family）。仅显式传入时写/更新；
    # _promote 自动建族时不传 → 保留已有值或置 None（人可后补），绝不覆盖已写好的中文名。
    if display_name:
        doc["display_name"] = display_name
    elif "display_name" not in doc:
        doc["display_name"] = None
    _write_json_atomic(p, doc)
    _OVERLAY_CACHE.pop(family, None)   # 失效内容缓存 → 下次 classify 读到新值


def _promote(family: str, host: str, topic_id: str) -> bool:
    """记 LLM-judged-official 一次；同族同 host 跨 ≥ PROMOTION_THRESHOLD 个不同
    topic 被判 → 晋升进 overlay。返回 True 当且仅当本次触发了晋升。

    幂等且同 topic 去重：topics 列表存放已计数的 topic_id，重复不再 +count。
    """
    if not family or not host:
        return False
    log = _read_promotion_log()
    fam = log.setdefault(family, {})
    entry = fam.setdefault(host, {"count": 0, "topics": [], "promoted": False})

    if topic_id not in entry["topics"]:
        entry["topics"].append(topic_id)
        entry["count"] = len(entry["topics"])

    did_promote = False
    if not entry["promoted"] and entry["count"] >= PROMOTION_THRESHOLD:
        _append_to_overlay(family, host)
        entry["promoted"] = True
        did_promote = True

    _write_json_atomic(_promotion_log_path(), log)
    return did_promote
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd /Users/yangqi/investing && python3 -m pytest prism/scripts/test_whitelist_overlay.py -v -k promote`
Expected: 4 passed。

- [ ] **Step 5: Commit**

```bash
cd /Users/yangqi/investing
git add prism/scripts/web_prescan.py prism/scripts/test_whitelist_overlay.py
git commit -m "feat(prism): _promote 晋升回路+promotion log（F4 收敛回路 4/4）"
```

---

## Task 5: 接线 —— register/extract 走 family-aware + 晋升钩子

**Files:**
- Modify: `prism/scripts/web_prescan.py:241`（`extract_url_features` 签名 + `:265` 调用）
- Modify: `prism/scripts/web_prescan.py:443-478`（`register_web_search_result` 内 family 派生 + 晋升钩子）
- Test: `prism/scripts/test_whitelist_overlay.py`

主 agent 不传任何新参数：`register_web_search_result` 从已有 slug+variant 自派生 family。`extract_url_features` 加可选 `slug/variant`（urls-only 旧调用兼容）。

- [ ] **Step 1: Write the failing test**

追加（端到端：两个不同 topic 各判一次 official → 第二次触发晋升，且全程不读 overlay 文件）：

```python
def test_register_promotes_across_two_topics(isolated_state, monkeypatch, tmp_path):
    # 让 _family_of 对两个 topic 都解析到同族 'cn-innovative-drug'
    def fake_read_topic(slug, variant):
        if slug == "cn-innovative-drug":
            return {"type": "industry", "parent_topic": None}
        return {"type": "arena", "parent_topic": "cn-innovative-drug"}
    monkeypatch.setattr(wp.topic_io, "read_topic", fake_read_topic)
    # 把会写盘的下游全 stub 掉，只验证晋升回路
    monkeypatch.setattr(wp, "find_by_url", lambda *a, **k: None)
    monkeypatch.setattr(wp, "_web_search_inbox_dir", lambda slug: tmp_path)
    monkeypatch.setattr(wp, "make_search_meta", lambda **k: {})
    monkeypatch.setattr(wp, "add_material", lambda **k: "mat-test")

    url = "https://hengrui.com/investor/x"
    common = dict(query="q", url=url, title="t", snippet="s",
                  addresses=["scope"], domain_tier="llm-judged-official")
    # topic 1 判一次 → 未达阈值
    wp.register_web_search_result(slug="cn-innovative-drug", variant="opus4.8", **common)
    assert wp.classify_domain(url, family="cn-innovative-drug") == "other"
    # topic 2（子 arena，同族）判一次 → 达阈值 → 晋升
    wp.register_web_search_result(slug="adc-arena", variant="v1", **common)
    assert wp.classify_domain(url, family="cn-innovative-drug") == "whitelist"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd /Users/yangqi/investing && python3 -m pytest prism/scripts/test_whitelist_overlay.py::test_register_promotes_across_two_topics -v`
Expected: FAIL —第二次 assert 仍为 `"other"`（钩子未接，晋升从未发生）。

- [ ] **Step 3: Write minimal implementation**

(a) `register_web_search_result`：把 `:476-478`

```python
    domain = _domain_of(url)
    if domain_tier is None:
        domain_tier = classify_domain(url)
```

替换为：

```python
    domain = _domain_of(url)
    family = _family_of(slug, variant)
    if domain_tier is None:
        domain_tier = classify_domain(url, family)
    elif domain_tier == "llm-judged-official" and family:
        # 主 agent 显式判权威 → 喂收敛回路（跨 topic 达阈值自动晋升进 overlay）
        _promote(family, domain, f"{slug}/{variant}")
```

> 设计取舍：仅当 caller **显式**传 `domain_tier='llm-judged-official'` 时才 `_promote`——这正是 H2 救回路径。`domain_tier=None` 自动判出的 whitelist/other 不进 log（避免把全局表命中也刷进 overlay）。

(b) `extract_url_features`：签名 `def extract_url_features(urls: list[str]) -> dict[str, dict]:`（`:241`）改为：

```python
def extract_url_features(
    urls: list[str], slug: str | None = None, variant: str | None = None
) -> dict[str, dict]:
```

并把 `:265` 的 `"in_whitelist": classify_domain(url) == "whitelist",` 改为：

```python
            "in_whitelist": classify_domain(url, _fam) == "whitelist",
```

在该函数 `out: dict[str, dict] = {}`（`:262`）之前加一行解析（slug 缺省 → None → 全局表 only，兼容旧调用）：

```python
    _fam = _family_of(slug, variant) if slug else None
```

- [ ] **Step 4: Run test to verify it passes（含既有 batch/feature 测试不回归）**

Run: `cd /Users/yangqi/investing && python3 -m pytest prism/scripts/test_whitelist_overlay.py prism/scripts/test_url_features.py prism/scripts/test_web_prescan_batch.py -v`
Expected: 全 passed（新增端到端 1 条 + 既有 url_features / batch 全绿）。

- [ ] **Step 5: Commit**

```bash
cd /Users/yangqi/investing
git add prism/scripts/web_prescan.py prism/scripts/test_whitelist_overlay.py
git commit -m "feat(prism): register/extract 接 family-aware+晋升钩子（F4 接线）"
```

---

## Task 6: 冷启动种子回填脚本（cn-innovative-drug）

**Files:**
- Create: `prism/scripts/seed_overlay_cn_pharma.py`
- Test: `prism/scripts/test_whitelist_overlay.py`（验证种子函数纯逻辑，写真实 state 由手动 run 完成）

把 F4 实测、当前仍判 `other` 的 11 个权威 host 一次性种进 `cn-innovative-drug` overlay（族 key = 该 industry 根 topic 自身 slug），并在 log 标 `promoted=True`，免去该族第 1 个 topic 的全量手判。

**种子 host（已核实当前 classify=other）：**
- 通用 CN 金融/交易所/媒体：`hkexnews.hk` `news.cn` `thepaper.cn` `eeo.com.cn` `pdf.dfcfw.com` `eastmoney.com`
- 药企 IR 根域（F4 明列）：`hengrui.com` `akesobio.com` `kelun-biotech.com` `innoventbio.com` `beigene.com`

- [ ] **Step 1: Write the failing test**

追加（测纯函数 `build_seed`，不写真实盘）：

```python
def test_seed_build_targets_only_missing(monkeypatch):
    import prism.scripts.seed_overlay_cn_pharma as seed
    # 全部 host 都应是当前全局表判不出的（=种子有意义）
    for h in seed.SEED_HOSTS:
        assert wp.classify_domain("https://" + h + "/x") == "other", h
    assert seed.FAMILY == "cn-innovative-drug"
    assert seed.DISPLAY_NAME == "中国创新药"
    assert len(seed.SEED_HOSTS) == 11


def test_seed_writes_display_name(isolated_state):
    import prism.scripts.seed_overlay_cn_pharma as seed
    seed.seed()
    import json as _json
    doc = _json.loads(wp._overlay_path("cn-innovative-drug").read_text(encoding="utf-8"))
    assert doc["display_name"] == "中国创新药"
    assert len(doc["hosts"]) == 11
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd /Users/yangqi/investing && python3 -m pytest prism/scripts/test_whitelist_overlay.py::test_seed_build_targets_only_missing -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'prism.scripts.seed_overlay_cn_pharma'`。

- [ ] **Step 3: Write minimal implementation**

创建 `prism/scripts/seed_overlay_cn_pharma.py`：

```python
"""一次性冷启动种子：把 PRISM_VALIDATION F4 实测的 CN 创新药权威源灌进
cn-innovative-drug overlay + 标 promotion log promoted。

族 key = 'cn-innovative-drug'（该 industry 根 topic 自身 slug；其 arena/company
子 topic 的 parent_topic 指向它，自动共享本 overlay）。

用法：  python3 -m prism.scripts.seed_overlay_cn_pharma
幂等：重复跑只会确保这些 host 在 overlay 内，不重复膨胀。
"""
from __future__ import annotations

from prism.scripts import web_prescan as wp

FAMILY = "cn-innovative-drug"        # 机器键（英文 kebab，= 该 industry 根 topic 的 slug）
DISPLAY_NAME = "中国创新药"           # 给人看的中文标签，写进 overlay 的 display_name 字段

# 均已核实在全局 WHITELIST_DOMAINS 判为 'other'（=确为缺口）
SEED_HOSTS = [
    # 通用 CN 金融/交易所/媒体（全局表缺，本族先补；后续可提案进代码级全局表）
    "hkexnews.hk", "news.cn", "thepaper.cn", "eeo.com.cn",
    "pdf.dfcfw.com", "eastmoney.com",
    # 药企 IR 根域（F4 明列）
    "hengrui.com", "akesobio.com", "kelun-biotech.com",
    "innoventbio.com", "beigene.com",
]


def seed() -> dict:
    """写 overlay + log（promoted）。返回 {'family','added','overlay_path'}。"""
    log = wp._read_promotion_log()
    fam = log.setdefault(FAMILY, {})
    for h in SEED_HOSTS:
        wp._append_to_overlay(FAMILY, h, display_name=DISPLAY_NAME)  # 写 overlay（幂等 union）
        entry = fam.setdefault(h, {"count": 0, "topics": [], "promoted": False})
        entry["promoted"] = True                  # 标已晋升，避免重复回流
        if "seed:F4" not in entry["topics"]:
            entry["topics"].append("seed:F4")
            entry["count"] = max(entry["count"], wp.PROMOTION_THRESHOLD)
    wp._write_json_atomic(wp._promotion_log_path(), log)
    return {
        "family": FAMILY,
        "added": sorted(SEED_HOSTS),
        "overlay_path": str(wp._overlay_path(FAMILY)),
    }


if __name__ == "__main__":
    import json
    print(json.dumps(seed(), ensure_ascii=False, indent=2))
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd /Users/yangqi/investing && python3 -m pytest prism/scripts/test_whitelist_overlay.py::test_seed_build_targets_only_missing -v`
Expected: PASS。

- [ ] **Step 5: Run the seed against REAL state + verify**

Run:
```bash
cd /Users/yangqi/investing
python3 -m prism.scripts.seed_overlay_cn_pharma
python3 -c "
from prism.scripts.web_prescan import classify_domain
for h in ['hkexnews.hk','hengrui.com','beigene.com','pdf.dfcfw.com']:
    print(classify_domain('https://'+h+'/x', family='cn-innovative-drug'), h)
"
```
Expected: seed 打印 JSON（added 11 项 + overlay_path）；4 行全部打印 `whitelist`。文件落在 `prism/state/whitelist/overlays/cn-innovative-drug.json`（含 `"display_name": "中国创新药"`）与 `prism/state/whitelist/_promotion_log.json`。可 `cat` overlay 文件确认中文标签已写入。

- [ ] **Step 6: Commit（含种子产物）**

```bash
cd /Users/yangqi/investing
git add prism/scripts/seed_overlay_cn_pharma.py prism/scripts/test_whitelist_overlay.py \
        prism/state/whitelist/overlays/cn-innovative-drug.json \
        prism/state/whitelist/_promotion_log.json
git commit -m "feat(prism): cn-innovative-drug overlay 冷启动种子（F4 回填 11 源）"
```

---

## Task 7: 文档接线 + 全量验证 + detect_changes

**Files:**
- Modify: `prism/workflows/_web_prescan_shared.md`（H2 救回段，约 `:121-123`）
- Test: 全量回归

让 workflow 文档点明"救回判 `llm-judged-official` 会自动喂域族 overlay，跑熟后同族手判递减；主 agent 无需 Read overlay/log"。

- [ ] **Step 1: 在 `_web_prescan_shared.md` 的救回模式说明（`:121` 那条）后补一句**

Read `prism/workflows/_web_prescan_shared.md:118-124` 定位救回段，在 `domain_tier='llm-judged-official'` 救回说明后追加：

```markdown
> 4. **域族收敛（F4）**：救回时带 `domain_tier='llm-judged-official'` 的 host，会被脚本按域族（`parent_topic||slug`）累计；同族跨 ≥2 个 topic 判为权威即自动晋升进 `prism/state/whitelist/overlays/{family}.json`，下次同族 topic 直接命中、不必再救。**这是脚本侧自动行为，主 agent 无需也不要 Read overlay/log 文件**（它们是 oracle 数据，非上下文材料）。
```

- [ ] **Step 2: 全量回归（本特性 + 相邻既有套件）**

Run:
```bash
cd /Users/yangqi/investing && python3 -m pytest \
  prism/scripts/test_whitelist_overlay.py \
  prism/scripts/test_whitelist_domains.py \
  prism/scripts/test_url_features.py \
  prism/scripts/test_web_prescan_batch.py -v
```
Expected: 全 passed，无 skip/error。

- [ ] **Step 3: gitnexus detect_changes（CLAUDE.md 强制）**

Run: 调用 `gitnexus_detect_changes()`（scope=unstaged 或 staged）。
Expected: changed symbols 仅落在 `classify_domain` / `register_web_search_result` / `extract_url_features` + 新增 `_matches`/`_load_overlay`/`_family_of`/`_promote`/`seed`；affected processes 风险 LOW。若出现预期外的高风险 process，停下报告用户。

- [ ] **Step 4: Commit**

```bash
cd /Users/yangqi/investing
git add prism/workflows/_web_prescan_shared.md
git commit -m "docs(prism): _web_prescan_shared 标注 F4 域族 overlay 收敛行为"
```

---

## Self-Review

**1. Spec coverage（对照前三轮讨论的设计）：**
- 三零件——族身份 `_family_of`(T3) / overlay 存储 `_load_overlay`+`_overlay_path`(T1) / 收敛回路 `_promote`+log(T4) ✓
- "脚本当 oracle、不进上下文"——主 agent 仅调既有 `register_web_search_batch` / `extract_url_features`，零新参数、不 Read 文件；接线 T5 + 文档 T7 ✓
- "全局表不动、族 overlay 分层"——`classify_domain` 命中顺序 全局→overlay→IR，family=None 向后兼容 T2 ✓
- "prescan 阶段 family 可用"——用 `parent_topic||slug` 而非 sidecar cluster_tags，T3 已据此设计 ✓
- 阈值 N=2 + 同 topic 去重防刷 + 已晋升幂等——T4 四个测试覆盖 ✓
- 冷启动种子回填——T6，11 个已核实 missing host，幂等 ✓

**2. Placeholder scan：** 无 TBD/TODO；每个 code step 含完整代码；命令含预期输出。`import json` 重复风险已显式提示先 grep 确认。

**3. Type consistency：**
- overlay 文件 schema `{family, display_name, hosts:list, promoted_count}` 在 `_load_overlay`(只读 `hosts`，忽略 `display_name`)/`_append_to_overlay`(写四键，`display_name` 仅显式传入时更新、不覆盖)/seed(写中文 `display_name`) 一致。机器键 `family` 仍是英文 kebab；`display_name` 纯人读标签。
- log schema `{family:{host:{count, topics:list, promoted:bool}}}` 在 `_promote`/`_read_promotion_log`/seed 一致。
- `_family_of(slug, variant) -> str|None`、`classify_domain(url, family=None)`、`_matches(domain, hosts)`、`_load_overlay(family)`、`_promote(family, host, topic_id)->bool`、`_append_to_overlay(family, host)`、`_write_json_atomic(path, data)` 全程签名一致，跨 T1/T4/T5/T6 调用匹配。
- `extract_url_features(urls, slug=None, variant=None)` 内部 `_fam` 命名与使用一致。

**已知边界（设计内、非缺陷）：**
- 冷启动税仍在——每个**新域族**第 1 个 topic 仍全量手判；本回路只摊薄"同族第 2 个起"。
- 通用 CN 源（hkexnews/dfcfw/news.cn 等）暂落在 pharma overlay；可后续单独提案晋升进代码级全局 `WHITELIST_DOMAINS`（reviewed PR），不阻塞本特性。
- 族粒度 = `parent_topic||slug`，比 cluster_tags 粗；同主题不同 geo（cn- / us-innovative-drug）各自独立 overlay（可接受，避免跨族污染）。
