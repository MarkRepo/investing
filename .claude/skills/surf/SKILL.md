---
name: surf
description: 趋势跟随系统（主升浪/二波）。触发词：surf / surf 扫描 / 本周扫描 / 趋势扫描 / 复查卡片 / 看看趋势 / 趋势在哪。发现当下正在发生的产业趋势，用市场资金行为交叉验证，产出带完整理由与证伪条件的趋势卡片。与 prism 价值研究系统无逻辑关系。
---

# surf — 趋势跟随系统

方法论权威版在 `surf/DESIGN.md`。**本文件是操作流程，不复制方法论**——判定标准、卡片格式、数据陷阱一律引用 DESIGN，避免两处分叉。

## 路由

| 用户说 | 执行 |
|---|---|
| 「surf 扫描」/「本周扫描」/「趋势扫描」 | 完整跑 Step 0 → 4 |
| 「复查卡片」/「卡片还成立吗」 | 只跑 Step 0 |
| 「surf 卡片 {slug}」 | 读 `surf/cards/{slug}/card.md` 与 `log.md` |
| 「加扫 {方向}」（重大事件） | 跑 Step 1 → 4，跳过 Step 0 |

## 铁律（违反即系统失效）

1. **Step 0 不可跳过。** 旧仓位的风险优先于新机会。
2. **Step 1 完成落盘之前，禁止查看任何行情数据。** 顺序颠倒会让系统退化为「涨了就找理由」的事后合理化机器（DESIGN §4/§5）。
3. **没有可观测证伪条件的卡片不得发出。**
4. 脚本一律用 `.venv/bin/python`，系统 python3 静默失败。
5. 查 A 股一致预期/盈利预测**不要用 WebSearch**（返回幻觉散文），用 tavily / exa / serper。

---

## Step 0 · 复查已有卡片

```bash
ls surf/cards/*/card.md
```

对每张卡片，逐条核对⑦证伪条件。分三类处置：

| 结果 | 动作 |
|---|---|
| 触发证伪 | **离场**，`log.md` 追加一条，卡片状态改为「已离场」并写明触发了哪条 |
| 接近触发 | `log.md` 记录预警，下次扫描重点跟 |
| 未触发 | `log.md` 追加一行当期数值快照 |

`log.md` **只追加，不修改历史记录**——向前验证是本系统唯一的有效性证据来源（DESIGN §3）。

---

## Step 1 · 通道 A 产业扫描

> ⚠️ **本步骤禁止查看行情、ETF 涨跌、持仓盈亏。**
> 先落笔，带时间戳，写入 `surf/scans/{today}/channel_A_industry_scan.md`，**写完才允许进入 Step 2**。

扫描当下真实发生的产业变化，不是复述已知叙事：

- 政策落地、技术拐点、供需反转、产品价格异动
- 产能周期、大厂资本开支、订单与排产、渠道反馈

先确立**宏观前提**（利率方向、油价、汇率、重大地缘事件），它是过滤器。扫描 #001 的教训：在加息 + 高油价环境下，只有业绩能真实兑现的趋势才走得完，长久期资产（电力、公用事业、核电）产业逻辑再硬也被贴现率压垮。

每个候选趋势要写明：驱动力类型、证据链（可核实事实）、阶段（0→1 / 1→10 / 10→100）、**主要瑕疵**。

**找不到反面证据 = 调研不够，不是趋势很完美。**

最后如实记录本次扫描的局限（信息源质量、覆盖盲区）。

---

## Step 2 · 技术佐证

```bash
.venv/bin/python surf/scripts/scan_cn.py    # A股 ETF + 板块资金流/广度/概念
.venv/bin/python surf/scripts/scan_us.py    # 美股 ETF
```

产出 `surf/scans/{today}/` 下的 `cn_etf.csv`、`us_etf.csv`、`cn_boards.csv`、`cn_board_summary.csv`、`cn_concepts.csv`。

### 检查清单

对 Step 1 的每个候选趋势，找到对应 ETF，逐项看。**不设阈值**——出手与否由 2×2 定性判定（DESIGN §6.2），但每一项都必须看过。

| 列 | 要问的问题 |
|---|---|
| `ret5` / `ret60` + `RS5` / `RS60` | 长短期是否一致？只有 ret5 强 = 超跌反弹；只有 ret60 强 = 可能在滞涨 |
| `位置分位` / `距250日高` | 是否已在追高区？位置极低（<10）说明资金根本没进来 |
| `趋势结构` | 空头排列下的上涨是反弹，不是趋势 |
| `量能比` | **< 1（未放量）时，必须说明为何仍判技术确认，并降低该卡片优先级** |
| `板块5日资金净额_亿` | 涨但资金净流出 = 散户游资推动，主力在出货 |
| `板块当日广度_%` | 低广度 + 高涨幅 = 少数权重股伪装成板块行情 |
| `折算修正` | 非 0 说明该标的历史经过份额折算复权，数值可信但需留意 |
| 跨市场 | A 股/港股/美股同方向是否共振？单市场独涨要警惕 |

**广度不能单独用**：它是当日快照，当日普涨不等于趋势，必须与 ret5/ret60、资金流同看（DESIGN §6.2）。

**别忘了检查阶段**：产业逻辑成立 + 技术确认，但 `ret250` 已经 +500%，那是成熟期，不是机会（DESIGN §2）。

---

## Step 3 · 通道 B 反查

看 `cn_etf.csv` / `us_etf.csv` 按 RS5、RS60 排序的榜首，**挑出 Step 1 没覆盖到的方向**，反查产业逻辑。

- 找得到真实产业变化 → 进入 2×2 判定，可能是通道 A 漏掉的趋势
- **找不到 → 判定纯炒作，写入 `surf/blacklist.yaml`**（含判定日期、代表标的、表面现象、判定理由、重新评估条件）

这一步是系统的免疫机制。扫描 #001 中通道 A 完整漏掉了当时美股最强主线，全靠这一步补回。

---

## Step 4 · 填 2×2，产出卡片

判定矩阵见 DESIGN §2。只有左上角（产业逻辑成立 + 技术确认）出手。

把本次判定写入 `surf/scans/{today}/summary.md`，需包含：
- 本次数据质量问题（若有新发现，同步补进 DESIGN §6.4）
- 市场整体状态（指数、量能背景）
- 每个候选趋势的技术佐证与判定理由
- 通道 B 的发现
- 2×2 填表
- **本次扫描修正了产业扫描的哪些结论**（留痕，不粉饰）

进入左上角的方向，产出/更新 `surf/cards/{slug}/card.md`，七字段格式见 DESIGN §7。

新建卡片时同步建 `log.md`，记录建卡时的关键数值、当时知道什么、**我可能错在哪**、下次该检查什么。

---

## 数据源备忘

| 用途 | 接口 | 注意 |
|---|---|---|
| A 股 ETF 列表 | `fund_etf_category_sina(symbol="ETF基金")` | 需清代理 |
| A 股 ETF 历史 | `fund_etf_hist_sina` | 需清代理；**非线程安全，必须串行**；未复权，脚本已处理折算 |
| 行业资金流 | `stock_fund_flow_industry("5日排行")` | 同花顺，90 行业 |
| 行业广度 | `stock_board_industry_summary_ths()` | 当日快照，含涨跌家数 |
| 概念资金流 | `stock_fund_flow_concept("5日排行")` | 387 概念，无历史指数 |
| 美股 | yfinance | **需要代理**，与 akshare 相反 |

东财 push2 端口（`fund_etf_spot_em` 等）在本机持续不可用，A 股板块数据一律走同花顺。
同花顺**板块指数历史有前视偏差，绝不可用于任何历史验证**（DESIGN 附录 A）。
