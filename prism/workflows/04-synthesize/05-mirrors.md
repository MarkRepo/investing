# 产出 05 — 历史镜像 (Historical Mirrors)

**定位**：找出与当前情景最相似的历史案例（成功+失败），从中提炼类比与警示，治幸存者偏差
**训练知识比例**：约 90%（LLM 超能力所在，几乎不依赖资料）
**产出文件**：`prism/topics/{slug}/{variant}/outputs/05_historical_mirrors.md`

---

## Step 0：前置检查（见 _shared.md）

---

## Step 1：识别 3-5 个历史类比（成功案例）

类比维度：产业结构（供需格局）、技术替代曲线、政策驱动周期、估值叙事泡沫、地理市场复制。

对每个类比案例：

### 案例名称：{国家} {行业/公司} {年代}

**类比逻辑**（哪里最像）：
- 供需结构
- 政策环境
- 估值叙事
- 参与者行为

**类比局限**（哪里不像，为什么不能完全照搬）：
- 结构差异
- 时代背景差异

**历史结果**：
- 发生了什么
- 峰值/谷值时间与幅度
- 触发拐点的事件

**对当前的启示**：
- 如果历史重演，当前应该…
- 历史中哪个信号最早出现，现在是否已经出现

---

## Step 1.5：失败镜像（强制 ≥ 2 个）

**关键约束**：如果你只能想到成功案例，写"我没找到合适的失败案例"本身就是 red flag，应当停止并要求用户补充失败案例资料。

找 **≥ 2 个**「曾经也长得像今天的标的，但最终失败」的历史案例：

### 失败案例 1：{国家} {行业/公司} {年代}

**当年的相似性**（哪里和今天像）：
- 叙事相似度
- 估值水位相似度
- 政策/资金面相似度

**最终结局**：
- 失败模式：{颠覆 / 周期顶 / 政策反转 / 现金流断裂 / 竞争击穿 / ...}
- 损失幅度（峰谷跌幅）：{X}%
- 触发崩塌的具体事件：{事件}

**对当前的警示**：
- 当年最早的预警信号是什么？
- 现在是否已经出现类似信号？
- 如果出现，如何应对？

### 失败案例 2：{国家} {行业/公司} {年代}

（同上结构）

---

## Step 2：类比综合与当前定位（必须包含对偶平衡）

综合所有类比案例（成功+失败），现在最像哪个历史时刻？

- 最相似的成功时刻：{描述}
- 最相似的失败时刻：{描述}
- **成功镜像如果实现，潜在回报区间：+X% 到 +Y%**
- **失败镜像如果实现，潜在损失区间：-X% 到 -Y%**
- 现在更像哪一类？理由（≤ 200 字）：
- 最大的差异点（不能类比的地方）：

---

## Step 2.5：填写 data_freshness

在 frontmatter 写入：
- `data_freshness`: 用到的最晚数据所在期（季度/月份）
- `data_freshness_basis`: 该期来自哪份 finding

---

## Step 3：写入文件 + 更新状态

output_key = `05_historical_mirrors`

---

## Step 4：汇报

```
✅ 历史镜像已生成 → v{N}
最佳成功类比：{案例名称} ({相似度})
最佳失败警示：{案例名称}
成功潜在回报：+{X}% ~ +{Y}%
失败潜在损失：-{X}% ~ -{Y}%
当前更像：{成功/失败/都不像}
最重要的历史教训：{一句话}
```
